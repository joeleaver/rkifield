//! rkf-convert — offline mesh-to-.rkf conversion CLI.
//!
//! Stubbed pending v2 rewrite. In v2, converts to .rkf v2 format
//! with per-object brick maps and layered LOD.
//! Will be rewritten in Phase 12 (v2-12.5).

fn main() {
    eprintln!("rkf-convert: stubbed pending v2 rewrite (Phase 12)");
    std::process::exit(1);
}
