//! Zone component — tab bar + active tab content.
//!
//! Stub — will be fully implemented in Phase C.

use rinch::prelude::*;

/// Renders a single zone. Placeholder for Phase C.
#[component]
pub fn ZoneComponent() -> NodeHandle {
    rsx! { div {} }
}
